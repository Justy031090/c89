#ifndef __WS2_H__
#define __WS2_H__
#include <stdio.h>

void SwapInt(int *val1, int *val2);

int *CopyArray(int array[], int size);

void PrintAddresses();

int SwapwTwoSize_t(size_t *ptr1, size_t *ptr2);

int SwapTwoPointers(size_t **ptr1, size_t **ptr2)

#endif /* __WS2_H__ */
